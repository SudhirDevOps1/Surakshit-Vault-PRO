{
  "name": "surakshit-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute surakshit-vault-db --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute surakshit-vault-db --file=./schema.sql --remote"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "@tsndr/cloudflare-worker-jwt": "^3.1.0"
  },
  "devDependencies": {
    "wrangler": "^3.90.0",
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.0"
  }
}
